/** Typed segment before the hero rotator. */
export const HERO_HEADLINE_PREFIX = 'Cedric is a product designer building things worth '

/** Words cycled after the prefix (natural grammar with `worth`). */
export const HERO_HEADLINE_ROTATOR_WORDS = [
  'having.',
  'keeping.',
  'using.',
  'shipping.',
] as const
