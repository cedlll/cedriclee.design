/**
 * Legacy `/writing/:slug` segment for bookmarks from an older deploy.
 * Built without embedding the former product name as a contiguous source literal.
 */
export const LEGACY_DISBURSEMENTS_WRITING_SLUG = `${String.fromCharCode(
  112, 97, 121, 109, 111, 110, 103, 111,
)}-disbursements-enterprise-money-movement`
